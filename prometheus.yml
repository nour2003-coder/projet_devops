global:
  scrape_interval: 5s          # fréquence de scrap des métriques
  evaluation_interval: 5s      # fréquence d'évaluation des règles

scrape_configs:

  # 🌱 Spring Boot app (EventsProject)
  - job_name: 'events_app'
    metrics_path: '/events/actuator/prometheus'
    static_configs:
      - targets: ['app:8089']
        labels:
          application: 'events-project'

  # 📦 cAdvisor : métriques des conteneurs Docker
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']

  # 🖥️ Node-exporter : métriques de la VM / host
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

  # 🧠 Prometheus lui-même
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']
